<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_selected="true">
        <shape>
            <solid android:color="#000000" /> <!-- Black color when selected -->
            <corners android:topRightRadius="50dp" android:bottomRightRadius="50dp" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape>
            <solid android:color="#F5F5F5" /> <!-- Gray color when not selected -->
            <corners android:topRightRadius="50dp" android:bottomRightRadius="50dp" />
            <stroke android:width="1dp" android:color="#C0C0C0" />
        </shape>
    </item>
</selector>
